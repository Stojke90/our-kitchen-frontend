import { Grid, Typography } from "@mui/material";
import React from "react";

const About = () => {
  return (
    <Grid>
      <Typography paragraph variant="subtitle1" color="primary">
        This page is for a description of the site and what we do in the
        application
      </Typography>
    </Grid>
  );
};

export default About;
